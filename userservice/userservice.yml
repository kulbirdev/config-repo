spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: "true"
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.MySQL5Dialect"
  database:
    driverClassname: "com.mysql.jdbc.driver"
  datasource:
    url: "jdbc:mysql://localhost:3306/sharingitems"
    username: "root"
    password: "{cipher}bee1410e337f3c8a4d5d98004a0e2534f38501f6dcb624fd1895bceed9b6dfa0"
    testWhileIdle: "true"
    validationQuery: "SELECT 1"

security:
  basic:
    enabled: false

server:
  port: 8081
